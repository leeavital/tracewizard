apply plugin: "idea"
apply plugin: "eclipse"

allprojects {
  apply plugin: "idea"
  apply plugin: "eclipse"
  apply plugin: "java"
  apply plugin: "application"

  repositories {
    mavenCentral()
  }
}
